{
  "scripts": {
    "start": "server/server.js",
    "heroku-prebuild": "cd server",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "deploy:full": "git add . && git commit -m heroku && git push && git push heroku main"
  }
}